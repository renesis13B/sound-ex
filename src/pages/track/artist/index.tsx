import Layout from '../../../components/templates/Layout'

const TrackArtistIndex = () => {
  // const [response, error] = useSearchInteractor()
  // const { search } = useContext(StoreContext)
  // // searchX().then(r => console.log(r.data))
  // const title = {
  //   main: 'Search',
  //   sub: `検索結果： ${search}`,
  // }

  return (
    <Layout title={`SOUND EX - Search 検索結果： テスト`}>
      {/*<section>*/}
      {/*  {response && <IndexContents tracks={response} title={title} />}*/}
      {/*</section>*/}
    </Layout>
  )
}

export default TrackArtistIndex